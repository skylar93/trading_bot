# SAC Agent Configuration
# Role in ensemble: moderate risk, auto-entropy tuning
agent:
  type: sac
  learning_rate: 3.0e-4
  gamma: 0.99
  buffer_size: 100000
  batch_size: 256
  tau: 0.005
  ent_coef: "auto"
  learning_starts: 1000

ensemble:
  weight_init: 0.25
  role: moderate
